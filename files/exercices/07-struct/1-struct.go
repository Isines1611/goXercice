package main

import "fmt"

type color struct {
	// TODO: create the color by having 3 int named: 'red', 'blue' and 'green'
}

func main() {
	// TODO: initiate variable 'red', 'blue' and 'green'
	// red := color
	// blue :=
	// green :=

	if red.red == 255 && red.blue == 0 && red.green == 0 &&
		blue.red == 0 && blue.blue == 255 && blue.green == 0 &&
		green.red == 0 && green.blue == 0 && green.green == 255 {
		fmt.Println("All good.")
	}
}
